name: agrotics
description: AgroTICs UTPL

version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  qr_flutter: ^4.0.0
  http: ^0.13.5
  share_plus: ^2.1.2
  path_provider:
  flutter_barcode_scanner: ^2.0.0
  intl: ^0.17.0
  sqflite: ^2.0.2+1
  connectivity_plus: ^2.3.9
  rxdart: ^0.27.7
  flutter_bloc: ^8.1.1
  shared_preferences: ^2.0.8
  package_info_plus: ^3.0.2



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
